
const Course = () => {
    return (
        <div>
            <h1>Data Type</h1>
            <p>Data type is a classification of data that tells the compiler or interpreter how to interpret the data. In Python, there are several built-in data types, such as integers, floating-point numbers, strings, and booleans.</p>
        </div>
    )
}

export default Course;
